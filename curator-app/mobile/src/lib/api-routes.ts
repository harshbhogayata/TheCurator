/** Articles, briefs, preferences, saves, etc. */
export const CONTENT_API_PREFIX = "/api/mobile/v1";

/**
 * Auth, account, onboarding. Older Railway deploys mount these under /api/mobile
 * while content lives under /api/mobile/v1.
 */
export const AUTH_API_PREFIX = "/api/mobile";
