/** Cap OS font scaling on chrome UI so tab bars and pills stay usable. */
export const UI_MAX_FONT_SIZE_MULTIPLIER = 1.3;

/** Article body can scale slightly more for accessibility. */
export const ARTICLE_MAX_FONT_SIZE_MULTIPLIER = 1.5;
