# Shrink EAS upload — dev/QA only (not needed in APK)
.maestro/
.expo-test-bundle/
*.md
!README.md

# Managed workflow — EAS prebuilds native projects on the server
/android
/ios
android.local.bak/

# Dependencies are installed on EAS builders
node_modules/
